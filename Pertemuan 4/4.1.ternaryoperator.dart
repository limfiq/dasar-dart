import 'dart:io';

void main() {
  var nilai = 60;
  // String keterangan;
  String keterangan = nilai >= 75 ? 'Anda Lulus' : 'Anda Gagal';
  // if (nilai >= 75) {
  //   keterangan = 'Anda Lulus';
  // } else {
  //   keterangan = 'Anda Gagal';
  // }
  print(keterangan);
}
